apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cloudflare-ingress
spec:
  ingressClassName: traefik
  rules:
    - host: grafana.mozka.app
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kube-prometheus-stack-grafana.monitoring
                port:
                  number: 80
    - host: linkding.mozka.app
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: linkding.linkding
                port:
                  number: 9090
    - host: audio.mozka.app
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: audiobookshelf.audiobookshelf
                port:
                  number: 3005
